seed: 42
output_dir: artifacts/vlm/unsup_encoder_cifar100
model:
  model_id: /home/yaner/.cache/huggingface/hub/models--Qwen--Qwen2.5-VL-3B-Instruct/snapshots/66285546d2b821cf421d4f5eb2576359d3770cd3
  lora_path: artifacts/vlm/unsup_encoder_cifar10/best/lora   # warm-start from CIFAR-10
  gradient_checkpointing: true
  projection:
    in_dim: 2048
    mid_dim: 512
    out_dim: 256
data:
  datasets:
    - name: cifar100
      data_dir: /home/yaner/kris/warehouse/DiEC/data/cifar-100-python
      image_size: 64
      max_per_class: 30     # 30 * 100 classes = 3000 images, no labels used
  images_per_batch: 32
  batches_per_epoch: 150
  image_size: 64
training:
  method: dec
  k: 2
  epochs: 10
  lr: 3.0e-5
  weight_decay: 0.01
  grad_accum_steps: 4
  max_grad_norm: 1.0
  log_every: 20
  refresh_p_every: 50
